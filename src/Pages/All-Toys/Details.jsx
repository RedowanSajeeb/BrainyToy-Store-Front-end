// import React from 'react';

import { useLoaderData } from "react-router-dom";

const Details = () => {
    const detlData = useLoaderData()
    console.log(detlData);
    return (
        <div>
            
        </div>
    );
};

export default Details;